using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using DomainLayer;

namespace AdvancedProgrammingGroup9TestProject
{
    [TestClass]
    class StaffCustomerTestClass
    {
        /*
        CustomerModel customer;
        StaffModel staff;

        [TestInitialize]
        public void TestInitialize()
        {
            //customer
            //staff
            Staff = new StaffModel(StaffId.Now, Firstname.Now,Lastname.Now,Workinghours.now,Adressid.now,AddID(1));
            StaffID1 = new staffIDModel("SAI", 2, null);
            Create = new DatabaseCreateQueries();

            
            
        }

        [TestCleanup]
        public void TestCleanup()
        {
            customer = null;
            staff = null;
        }

        [TestMethod]
        public void StaffTesting()
        {
            //staff
            
             StaffId now = StaffID.Now;
            StaffId1 = new StaffIDModel(now, StaffId.Now.AddId(1));
            Assert.AreEqual(now, Firstname.GetReceivedFirstname(), "Received Firstname Incorrect");
            Assert.AreEqual(now, Lastname.GetReceivedlastname(), "Received Lastname Incorrect");
            Assert.AreEqual(now, Workinghours.GetReceivedworkinghours(), "Received Workinghours Incorrect");
            Assert.AreEqual(now, AddressId.GetReceivedAddressId(), "Received AddressId Incorrect");
            
            
        }

        [TestMethod]
        public void CustomerTesting()
        {
            //
        }
        */
    }
}
